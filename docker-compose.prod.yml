
services:
  api:
    build: .
    container_name: lastglance_backend_prod
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      - .env
    restart: always
    environment:
      - NODE_ENV=production
